# SEC EDGAR Integration Requirements
# These are lightweight dependencies, most likely already installed

# Core data processing
pandas>=1.3.0

# HTTP requests
requests>=2.26.0

# HTML/XML parsing
beautifulsoup4>=4.9.3
lxml>=4.6.3

# Note: All other dependencies (datetime, logging, time, etc.) are Python standard library
